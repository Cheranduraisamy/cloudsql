spring.application.name=demo
server.port=8080
# ===============================
# = DATA SOURCE
# ===============================
# Set here configurations for the database connection
# Use this URL for Google Cloud SQL (when deployed)
spring.datasource.url=jdbc:postgresql:///testDB?cloudSqlInstance=keen-ally-473016-s9:us-central1:cheran-database&socketFactory=com.google.cloud.sql.postgres.SocketFactory
# data source
# Use below URL for running locally
#spring.datasource.url=jdbc:postgresql://35.238.67.121:5432/testBD
spring.datasource.username=postgres
spring.datasource.password=Postgres@123
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update): with "create-drop" the database
# schema will be automatically created afresh for every start of application
spring.jpa.hibernate.ddl-auto=update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
